#[derive(Debug, Clone, PartialEq)]
pub enum 낱말 {
    정수(i64), 실수(f64), 문자열(String), 이름(String),
    티, 옴, 타,                    // 3진 리터럴
    확정, 미확인, 오해, 미인지,       // 인식상태
    변수, 함수, 반환, 만약, 아니면, 동안, 출력, 출력값,
    주장, 근거, 전이, 연결,          // 온톨로지
    판정, 양이면, 중이면, 음이면,     // 3방향 분기
    // 파일 I/O
    읽기, 쓰기, 덧쓰기, 파일존재,
    // 모듈
    가져오기,
    // 문자열
    글자수, 포함, 대문자, 소문자,
    글자, 부분, 숫자변환, 문자열변환,   // 셀프호스팅용
    // 배열
    추가, 길이, 설정,                   // 배열 조작
    // 기타
    각각,                           // for-each
    입력,                           // 사용자 입력
    더하기, 빼기, 곱하기, 나누기, 나머지기호,
    대입, 같음, 다름, 크다, 작다, 크거나같다, 작거나같다,
    부정, 그리고, 또는, 합의,
    왼괄호, 오른괄호, 왼중괄호, 오른중괄호, 왼대괄호, 오른대괄호,
    쉼표, 점, 끝,
}
